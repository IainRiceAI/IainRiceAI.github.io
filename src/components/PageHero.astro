---
const { eyebrow, title, lede } = Astro.props;
---
<section class="page-hero">
  <div class="container">
    <span class="eyebrow" style="color: var(--teal-bright);">{eyebrow}</span>
    <h1>{title}</h1>
    {lede && <p class="lede">{lede}</p>}
  </div>
</section>
